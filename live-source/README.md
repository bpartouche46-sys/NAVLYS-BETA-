# live-source/ — Source RÉELLE des sites live (capture depuis l'ancien PC)

Ce dossier reçoit la **vraie source de production** des projets Vercel,
capturée depuis l'ancien PC de Bruno (OneDrive `NAVLYS_SITE_COMPLET` / `NAVLYS_CENTRAL`).

But : mettre le code live **sous Git** (objectif Phase 0) AVANT de relier
Vercel ↔ GitHub, pour ne RIEN écraser (voix `navlys-alive.js` + toutes les pages).

⚠️ Aucun secret ici : pas de `.env`, pas de clé, pas de token (cf. `.gitignore`).
Procédure complète : `docs/PROCEDURE-VERCEL-GITHUB.md`.

Arborescence cible : `live-source/<projet>/...` (un sous-dossier par projet Vercel).
