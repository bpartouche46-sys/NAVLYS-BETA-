# recup-docs/ — Documents récupérés de l'ancien PC (OneDrive) pour ultrareview

Reçoit **uniquement les DOCUMENTS** (`.md`, rapports, prompts, stratégie, notes)
récupérés depuis `C:\Users\BP\OneDrive` — PAS le code (qui va dans `live-source/`),
PAS les médias/installeurs, PAS les secrets.

⚠️ INTERDIT ici : `recovery-codes`, fichiers `kgey`/`zi…` (clés), ORIAS/Outlook/photos,
`.env`, `.exe`, `.mp4`, archives.

But : ultrareview → extraire les points importants de chaque doc, dédoublonner,
remettre en ordre, consolider dans la mémoire projet (`docs/`).
