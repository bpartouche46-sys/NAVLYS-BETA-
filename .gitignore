# Secrets — ne JAMAIS committer
.env
.env.*
*.key
*.pem
secrets/
credentials*.json
# … mais on AUTORISE les modèles d'exemple (sans valeurs)
!.env.example
!*.env.example

# Dépendances
node_modules/
vendor/
.venv/
__pycache__/

# Build / sortie
dist/
build/
.next/
out/
.vercel/
.turbo/

# Système / éditeurs
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Réglages locaux Claude (perso, non partagés)
.claude/settings.local.json

# Core — fichiers locaux/secrets (on garde *.example, jamais les vrais)
core/.env
core/.mcp.json
core/logs/
core/dist/
